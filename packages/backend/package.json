{
  "name": "@dework/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "source .venv/bin/activate && uvicorn app.main:app --reload",
    "start": "uvicorn app.main:app",
    "test": "pytest"
  }
} 